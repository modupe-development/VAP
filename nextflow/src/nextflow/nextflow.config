includeConfig 'applications.config'

trace {
    enabled = true
    file = 'trace_pipeline.txt'
    fields = 'task_id,name,container,hash,status,exit,start,realtime,duration,%cpu,rss,vmem'
    raw = true
}

